MediaPlayerCommand
------------------
This is the command module for MediaPlayerHD; It shows how to run remote actions and 
send strings between applications using tethering components.

MediaPlayer has some standard actions registered as public (Play, Clear), so they 
can be executed remotely.